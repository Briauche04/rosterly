// app/manager/layout.tsx
'use client';

import type { ReactNode } from 'react';
import RosterlyShell from '@/app/rosterly/RosterlyShell';
import ManagerGate from './ManagerGate';

/**
 * Shell is IDENTICAL to the main page (top-nav, spacing, footer).
 * Only inner content (children) differs.
 */
export default function ManagerLayout({ children }: { children: ReactNode }) {
  return (
    <RosterlyShell>
      <ManagerGate>{children}</ManagerGate>
    </RosterlyShell>
  );
}
